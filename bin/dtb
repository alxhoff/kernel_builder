#!/bin/bash
# DTB / DTS decompile / search / verify helper
# Short alias for scripts/utils/dtb/dtb_dts_helper.sh. See bin/README.md for the full list.

set -e
REPO_ROOT="$(cd -- "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")/.." &> /dev/null && pwd)"
exec "$REPO_ROOT/scripts/utils/dtb/dtb_dts_helper.sh" "$@"
