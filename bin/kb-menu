#!/bin/bash
# Interactive menuconfig-style TUI over the kernel_builder workflows.
# See bin/README.md for the full alias list.

set -e
REPO_ROOT="$(cd -- "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")/.." &> /dev/null && pwd)"
exec "$REPO_ROOT/scripts/menu/kb-menu.sh" "$@"
